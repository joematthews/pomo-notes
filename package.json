{
  "scripts": {
    "format": "prettier --write \"**/*.{md,sh,json}\"",
    "prepare": "husky install"
  },
  "devDependencies": {
    "lint-staged": "^15.2.2",
    "prettier": "^3.2.5",
    "prettier-plugin-sh": "^0.14.0",
    "husky": "^8.0.0"
  }
}
